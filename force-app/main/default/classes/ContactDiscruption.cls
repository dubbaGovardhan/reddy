public class ContactDiscruption {
    public static void sendEmailToOpportuntyOwner(list<Opportunity> oprtny){
        list<id> idlist= new list<id>();
        set<id> setId = new set<id>();
        
        for(Opportunity opr:oprtny){
            if(opr.StageName =='closed won'){
                
            }
        }
        
    }
}